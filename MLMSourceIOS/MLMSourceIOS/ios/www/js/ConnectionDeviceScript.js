// Initialize your appvar alertApp = new Framework7({    modalTitle: "แจ้งเตือน !!"});var $checkNet = false;/// Check Internet Every Eventsfunction checkConnection() {    //alert(mobileApp);    if (mobileApp == true)    {        //$checkNet = true;        //return true;        var networkState = navigator.connection.type;        var states = {};        states[Connection.UNKNOWN] = 'Unknown connection';        states[Connection.ETHERNET] = 'Ethernet connection';        states[Connection.WIFI] = 'WiFi connection';        states[Connection.CELL_2G] = 'Cell 2G connection';        states[Connection.CELL_3G] = 'Cell 3G connection';        states[Connection.CELL_4G] = 'Cell 4G connection';        states[Connection.CELL] = 'Cell generic connection';        states[Connection.NONE] = 'No network connection';        //alert("Network = " + networkState.toString());        if (networkState.toString() == 'none' || networkState.toString() == 'unknow') {            //alertApp.alert('ไม่สามารถติดต่ออินเตอร์เน็ทของท่านได้');            navigator.notification.alert(                                         'ไม่สามารถติดต่ออินเตอร์เน็ทของท่านได้', //states[networkState],  // message                                         alertDismissed,         // callback                                         'Cannot Connect to Internet',            // title                                         'ตกลง'                  // buttonName                                         );            $checkNet = false;        }        else {            /* navigator.notification.alert(                                          states[networkState],  // message                                          alertDismissed,         // callback                                          'ประเภทของเครือข่าย',            // title                                          'ตกลง'                  // buttonName                                          );             */            $checkNet = true;            //hideKeyboard();        }    } else {        $checkNet = true;    }    hideKeyboard();        }function alertDismissed() {    MLMApp.hidePreloader();    return false;}